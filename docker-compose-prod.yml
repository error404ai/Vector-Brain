services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: vector-brain-prod
    restart: always
    env_file: .env
    ports:
      - '${PORT:-3002}:3002'
    environment:
      - NODE_ENV=production
    depends_on:
      db:
        condition: service_healthy
    networks:
      - vector-brain-network
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M

  db:
    image: mysql:8.0
    container_name: vector-brain-mysql-prod
    restart: always
    ports:
      - '${MYSQL_EXTERNAL_PORT:-3306}:3306'
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQLPASSWORD}
      - MYSQL_DATABASE=${DATABASE}
    volumes:
      - mysql_data_prod:/var/lib/mysql
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost', '-u', 'root', '-p${MYSQLPASSWORD}']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - vector-brain-network
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: vector-brain-phpmyadmin-prod
    restart: always
    ports:
      - '${PHPMYADMIN_PORT:-8082}:80'
    environment:
      - PMA_HOST=db
      - PMA_PORT=3306
      - PMA_USER=${MYSQLUSERNAME}
      - PMA_PASSWORD=${MYSQLPASSWORD}
      - UPLOAD_LIMIT=100M
    depends_on:
      db:
        condition: service_healthy
    networks:
      - vector-brain-network
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M

volumes:
  mysql_data_prod:

networks:
  vector-brain-network:
    driver: bridge
